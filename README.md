# scanning-ochestrator
A lightweight Python tool that automates vulnerability scanning using multiple Kali Linux security tools and generates a consolidated HTML report.  Instead of manually running several scanners one by one, this wrapper orchestrates them automatically, parses their outputs, and merges the results into a structured vulnerability report. 
